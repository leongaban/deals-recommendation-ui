// Test utils
import { testCommonComponentAttrs } from 'utils/tests';

// Components
import ResetPassword from './resetPassword';

describe('<ResetPassword /> component', () => {
  testCommonComponentAttrs(ResetPassword);
});
